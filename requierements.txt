Once we are connected to a python environement, we need to run the following code in the bash terminal :
pip install tensorflow==2.4.1 tensorflow-gpu==2.4.1 pixellib opencv-python

Then we need to download the file 'mask_rcnn_coco.h5' from here : https://github.com/matterport/Mask_RCNN/releases/tag/v2.0